eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka
server:
  port: 9002
spring:
  application:
    name: Appointment-Service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: examly
    url: jdbc:mysql://localhost:3306/AppointmentService
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
